import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rechargechap.test',
  appName: 'Recharge Chap',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
